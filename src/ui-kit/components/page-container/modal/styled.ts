import styled from "styled-components";
import { css } from "styled-components/macro";


export const SModalPageContainer = styled.div` && {
  ${({ theme }) => css`${theme.components.modalPageContainer.SModalPageContainer}`}
}`;

export const CloseIconWrapper = styled.div` && {
  ${({ theme }) => css`${theme.components.modalPageContainer.CloseIconWrapper}`}
}`;

import styled from "styled-components";


export const SDay = styled.input`
  ${({ theme }) => theme.components.controls.day.SDay}
`;

export const DText = styled.p`
  ${({ theme }) => theme.components.controls.day.DText}
`;

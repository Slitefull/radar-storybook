import { FC, memo } from 'react';


const RedirectTestingTable: FC = memo((): JSX.Element => {
  return (
    <div>
      RedirectTestingTable
    </div>
  );
});

export default RedirectTestingTable;